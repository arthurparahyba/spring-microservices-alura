
server:
  port: 8888
spring:
  cloud:
    config:
      server:
          encrypt:
            enabled: false
          git:
            uri: https://github.com/arthurparahyba/spring-microservices-alura/
            search-paths: config-repo/loja, config-repo/fornecedor, config-repo/transportador
      
